{% extends 'PerfilacBundle::index.html.twig' %}
{% block title %}Listar Tipo de Producto{% endblock %}
{% block body %}
<table id="listado_tipoProducto" summary="Tipo Producto" class="ganar_tabla_list ganar_tabla_linkcenter">
        <caption>Tipos de Producto</caption>
        <tr>
            <th scope="col">
                Nombre
            </th>
          {#   {% if is_granted('ROLE_ADMINISTRADOR') %}  #}
                <th scope="col">
                    Editar
                </th>
           {# {% endif %}#}
        </tr>
        {% for tipoProducto in tiposProducto %}
            <tr>
                <td>
                    {{ tipoProducto.nombre }}
                </td>
              {#   {% if is_granted('ROLE_ADMINISTRADOR') %}#}
                    <td>
                        <a href="{{ path('tipoProductoModificar', {'id':tipoProducto.id}) }}">Editar</a>
                    </td>
               {#  {% endif %}#}
            </tr>
        {% endfor %}
    </table>
    {#las clases css que se utilizaron en el paginador son de bostrap #}
    <div class="ganar_paginador_contenedor">
        {{ simple_paginator_render('tipoProductoListar', null, {
          'routeParams' : {},
          'container_class' : 'pagination pagination-sm',
          'firstDisabledClass':'disabled',
          'previousDisabledClass' : 'disabled',
          'currentClass':'active',
          'nextDisabledClass' : 'disabled',
          'lastDisabledClass':'disabled'
        },'PerfilacBundle::ganarSimplePaginador.html.twig')              
        }}  
    </div>
{% endblock %}  
