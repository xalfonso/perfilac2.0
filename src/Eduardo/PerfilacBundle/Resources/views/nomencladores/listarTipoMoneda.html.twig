{% extends 'PerfilacBundle::index.html.twig' %}
{% block title %}Listar Tipo de Moneda{% endblock %}
{% block body %}
<table id="listado_tipoMoneda" summary="Tipo Moneda" class="ganar_tabla_list ganar_tabla_linkcenter">
        <caption>Tipos de Moneda</caption>
        <tr>
            <th scope="col">
                Nombre
            </th>
          {#   {% if is_granted('ROLE_ADMINISTRADOR') %}  #}
                <th scope="col">
                    Editar
                </th>
           {# {% endif %}#}
        </tr>
        {% for tipoMoneda in tiposMoneda %}
            <tr>
                <td>
                    {{ tipoMoneda.nombre }}
                </td>
              {#   {% if is_granted('ROLE_ADMINISTRADOR') %}#}
                    <td>
                        <a href="{{ path('tipoMonedaModificar', {'id':tipoMoneda.id}) }}">Editar</a>
                    </td>
               {#  {% endif %}#}
            </tr>
        {% endfor %}
    </table>
    {#las clases css que se utilizaron en el paginador son de bostrap #}
    <div class="ganar_paginador_contenedor">
        {{ simple_paginator_render('tipoMonedaListar', null, {
          'routeParams' : {},
          'container_class' : 'pagination pagination-sm',
          'firstDisabledClass':'disabled',
          'previousDisabledClass' : 'disabled',
          'currentClass':'active',
          'nextDisabledClass' : 'disabled',
          'lastDisabledClass':'disabled'
        },'PerfilacBundle::ganarSimplePaginador.html.twig')              
        }}  
    </div>
{% endblock %}  
