{% extends 'PerfilacBundle::index.html.twig' %}
{% block title %}Listar Suministrador{% endblock %}
{% block body %}
<table id="listado_suministrador" summary="Suministrador" class="ganar_tabla_list ganar_tabla_linkcenter">
        <caption>Suministradores</caption>
        <tr>
            <th scope="col">
                Nombre
            </th>
          {#   {% if is_granted('ROLE_ADMINISTRADOR') %}  #}
                <th scope="col">
                    Editar
                </th>
           {# {% endif %}#}
        </tr>
        {% for suministrador in suministradores %}
            <tr>
                <td>
                    {{ suministrador.nombre }}
                </td>
              {#   {% if is_granted('ROLE_ADMINISTRADOR') %}#}
                    <td>
                        <a href="{{ path('suministradorModificar', {'id':suministrador.id}) }}">Editar</a>
                    </td>
               {#  {% endif %}#}
            </tr>
        {% endfor %}
    </table>
    {#las clases css que se utilizaron en el paginador son de bostrap #}
    <div class="ganar_paginador_contenedor">
        {{ simple_paginator_render('suministradorListar', null, {
          'routeParams' : {},
          'container_class' : 'pagination pagination-sm',
          'firstDisabledClass':'disabled',
          'previousDisabledClass' : 'disabled',
          'currentClass':'active',
          'nextDisabledClass' : 'disabled',
          'lastDisabledClass':'disabled'
        },'PerfilacBundle::ganarSimplePaginador.html.twig')              
        }}  
    </div>
{% endblock %}  
